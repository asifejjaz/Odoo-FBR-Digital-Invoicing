<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_company_form_inherit_fbr" model="ir.ui.view">
        <field name="name">res.company.form.inherit.fbr</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <field name="fbr_province_id"
                       help="Required for the FBR rate/SRO cascade on invoice lines - set this once so every invoice's Sale Type -> Rate -> SRO Schedule -> SRO Item lookups actually have a seller province to send FBR."/>
            </field>
        </field>
    </record>
</odoo>
